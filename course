#include <stdio.h>

int main(void) {
	int t,i,n,m,k;
	scanf("%d",&t);
	for(i=0;i<t;i++)
	{
	    scanf("%d %d %d",&n,&m,&k);
	    if(m>=k+n)
	        printf("yes \n");
	    else
	        printf("no \n");
	}
	return 0;
}
}

